apiVersion: v1
kind: Service
metadata:
  name: desxzt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "desxzt"
    port: 8080
  selector:
    app: desxzt
