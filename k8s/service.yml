apiVersion: v1
kind: Service
metadata:
  name: fgnbhy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fgnbhy"
    port: 8080
  selector:
    app: fgnbhy
