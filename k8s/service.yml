apiVersion: v1
kind: Service
metadata:
  name: kmntgvc
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "kmntgvc"
    port: 8080
  selector:
    app: kmntgvc
